---
project: ModelForge
prd: PRD_v11_score_to_8.md (no-spend dev SOTA push)
task: D7 (security hardening)
total_workstreams: 8 (D4, D1, D3 partial, D7, D8, D2, D6, D5)
status: in_progress
last_updated: 2026-05-15T13:30:17+02:00
---

<current_state>
Mid-execution on **PRD_v11_score_to_8.md** — the no-spend dev-only SOTA push to lift weighted score 5.54 → 7.25 (dev ceiling).

**Currently on D7** (security hardening). Just finished MOAT_SWEEP commit (8175aad) and was about to write LICENSE file (BSL 1.1 → MIT Change Date 2030 per prior memory decision) and start CI security hardening (Bandit + Safety + SLSA L3 + Sigstore).

**No work in flight uncommitted.** Working tree clean except for two test scaffolding artifacts safe to delete (`AUDIT_RUN_LISTED_smoke.md/.json`).
</current_state>

<completed_work>

### This session
- **Triple-AI IC review** (Opus 4.7 + ChatGPT 5T + Gemini partial)
  - Files: `TRIPLE_REVIEW_2026-05-15.md`, memory `modelforge_triple_review_2026_05_15.md`
  - Consensus: 5.53-5.62 weighted / $3.5-5M base pre-money
  - ChatGPT verdict identical to round-1 (5.42), pre-money DROPPED to €1.5-2.5M
  - Gemini Pro Deep Research timed out (8 min); Veloce/Flash partial — D1=9.5 only

- **PRD_v11_score_to_8.md** (revised twice)
  - First version: Phase A+B+C with €350-600K spend → 8.015 / $15-25M
  - Final version (current): no-spend dev-only ceiling → 7.255 / $8-12M

- **D4 (data integrations) — COMMIT b84002a**
  - YahooProvider class with quote+history+fundamentals+entity_lookup
  - Yahoo cookie/crumb anti-bot dance for fundamentals endpoint
  - Yahoo `financialData` module for EBITDA/totalDebt/totalCash
  - Live verified end-to-end: AAPL FY25 $416B revenue, ENEL.MI mcap €95B
  - LIVE providers: 3 → 4 (first one with quote/history/fundamentals)
  - 510/510 tests pass

- **D1 (formula discipline) — COMMIT 476ced0**
  - New Trust Layer rule `dcf_implied_equity_vs_market_cap` (FAIL severity)
  - New `Target.ticker` optional spec field
  - `examples/dcf_enel.yaml` updated with `ticker: ENEL.MI`
  - Live verified: Enel demo NOW FAILs at audit time with "DCF implied equity 553B vs live market cap 94.9B (+482%)" — Killer #1 OBJECTIVELY CLOSED
  - `scripts/audit_listed.py` (731 lines): 20-listed-co harness
  - `AUDIT_RUN_LISTED.md` + `.json`: 20/20 build success, 5 CLEAN, 12 MODERATE, 3 CATASTROPHIC (WFC, GS, PFE — all explained)

- **D3 (modelling completeness) PARTIAL — COMMIT 8175aad**
  - `scripts/moat_sweep.py` + `MOAT_SWEEP.md` scoreboard
  - 8/14 templates pass all 4 moat gates
  - 6 templates need orphan-named-range / density gap fixes (deferred — per-template wiring work)
</completed_work>

<remaining_work>

### D7 — Security / compliance (IN PROGRESS, ~+0.172 weighted lift)
- [ ] LICENSE file (BSL 1.1 → MIT Change Date 2030-05-15) — per memory `session_2026_05_15_modelforge_0_9_7.md` decision
- [ ] pyproject.toml: change `license = { text = "Proprietary" }` → `"BUSL-1.1"`
- [ ] THREAT_MODEL.md (STRIDE analysis — extends existing SECURITY.md)
- [ ] `.github/workflows/security.yml` — Bandit + safety + pip-audit (advisory)
- [ ] SLSA L3 build provenance via GitHub Actions
- [ ] Sigstore signing on releases (`publish.yml`)
- [ ] CVD policy publication (already in SECURITY.md, just needs surface)

### D8 — Regional coverage (~+0.148 weighted lift)
- [ ] Swiss / Dutch / Irish tax modules (currently 7 jurisdictions: IT/US/UK/DE/FR/ES/JP)
- [ ] Add 25 international listed-cos to audit harness (LSE, DB Frankfurt, SIX Swiss)
- [ ] IFRS / HGB / US-GAAP toggle in three_statement template
- [ ] Cross-border M&A FX hedging + withholding tax in merger template

### D2 — Source traceability (~+0.060 weighted lift)
- [ ] Auto-generate `lineage_walk.pdf` per workbook (with bbox annotations)
- [ ] SHA-256 hash chain on source documents
- [ ] Reproducibility test: same spec → same workbook hash
- [ ] CI test: rebuild from same spec, assert deterministic output

### D6 — Collaboration (~+0.212 weighted lift, HEAVY)
- [ ] Yjs CRDT cell-level sync over Cloudflare Workers Durable Objects (free)
- [ ] Comment threads anchored to named ranges
- [ ] 4-eyes reviewer approval workflow (state machine)
- [ ] Build/diff between workbook versions

### D5 — Productization / SaaS (~+0.443 weighted lift, HEAVIEST)
- [ ] Multi-tenant SaaS shell on Supabase free + Cloudflare Pages free + Stripe transactional
- [ ] Auth (Supabase Auth), tenant isolation (RLS), audit log table
- [ ] Hosted at modelforge.pages.dev (free CF subdomain)
- [ ] Billing scaffold (Stripe — free transactional, no fixed cost)

### D3 deep work (DEFERRED, ~+0.225 weighted lift if completed)
- [ ] Wire orphan named ranges in 6 templates (per-template work):
  - credit_memo_cdmo (8 orphans, 1 density gap)
  - merger_tim_iliad (9 orphans, 2 density gaps)
  - npl_mixed_portfolio (5 orphans, 1 density gap)
  - real_estate_pbsa (8 orphans)
  - sponsor_lbo_techco (10 orphans, 1 density gap)
  - unitranche_cdmo (7 orphans, 1 density gap)
</remaining_work>

<decisions_made>

- **PRD scope: no-spend ceiling at 7.25 weighted** — original PRD had Phase C with €350-600K spend + 1 BD hire targeting 8.015. User said "don't spend money" → revised to dev-only path capping at ~7.25 (above which needs users or capital).

- **License: BSL 1.1 → MIT Change Date 2030-05-15** — per prior memory entry `session_2026_05_15_modelforge_0_9_7.md` ("BSL 1.1 with Change Date 2030-05-15 → MIT was selected when first asked"). NOT plain MIT — honors the previous user choice.

- **Wedge selection deferred** — PRD recommends W3 (Italian PE/credit) but not blocking dev work. Actual selection happens when starting outreach.

- **Execution order by dependency × weighted-lift**: D4 (foundation, +0.315) → D1 (closes Killer #1, +0.144) → D3 (partial, +partial of 0.225) → D2/D8/D7 (mid-effort) → D6/D5 (heaviest, +0.212/+0.443). Currently mid-D7.

- **Python DCF compute in audit_listed.py** — bypasses `formulas` package limitation with cross-sheet/named-range refs. Documented in script docstring. Trust Layer rule still wired but harness uses Python compute for deterministic results.

- **D3 deep template fixes deferred** — wiring 6 templates' orphan named ranges is per-template work (~30-60 min each), too time-consuming for this session. MOAT_SWEEP.md tracks the gap; revisit after D5.
</decisions_made>

<blockers>

- **Yahoo crumb dance fragility**: cookie/crumb cached process-lifetime, may need refresh after ~24h Yahoo session expiry. Audit harness re-fetches each run, so per-run robust. Long-running daemons would need refresh.

- **`formulas` package limitation**: cannot resolve cross-sheet/named-range references in DCF template (Valuation!D21 etc. return None). Trust Layer rule for live mcap deviation thus silently skips when probing workbook. Worked around in `scripts/audit_listed.py` by computing DCF in Python directly. The Trust Layer rule still works when workbooks are recalculated by Excel/LibreOffice and re-saved with cached values.

- **Windows cp1252 encoding** breaks `python -m modelforge.cli --help` (chi character χ in help text). Workaround: `PYTHONIOENCODING=utf-8`. Functional commands (`build`, `qc`, `audit-all`) work fine. Pre-existing — not introduced this session.

- **D5 + D6 require web framework choices** that could constrain Phase-C SaaS architecture. Recommend: Next.js 15 + Tailwind (already partial in repo's `web/` dir) for D5; Yjs over CF Workers Durable Objects for D6. But final stack pick is reversible.

- **OIDC PyPI publishing** still fails on `invalid-publisher` (pre-existing). Manual twine works via `~/.claude/secrets/pypi.env`. Not blocking; release flow is fine.
</blockers>

<context>
The big strategic insight from this session: **ChatGPT round-2 said "stop adding breadth, fix reliability."** This dictates everything in PRD-v11 — no new templates, no new asset classes, no new exporters. Every workstream is either:
1. Validation work (D1 harness — proves what exists)
2. Productization (D5 SaaS, D6 collab — wraps what exists)
3. Compliance (D7 security, D2 lineage — formalizes what exists)
4. Coverage (D4 free adapters, D8 international — extends what exists shallowly)

The "no spend" constraint kills:
- Bloomberg/Refinitiv/CapIQ paid feeds (D4 capped at 7.0 not 8.5)
- Big-4 audit (D2 capped at 8.5 not 9.0+)
- SOC2 Type II (D7 capped at 5.5 not 7.5)
- Real paying users (D5 capped at 5.5 not 7.5)
- Real multi-user activity (D6 capped at 6.0 not 7.5)

Hence ceiling at ~7.25 weighted. Above that needs spend OR genuine customer pull (which itself needs founder time + outreach).

The IC review's 3 unlocks (Trust Layer ship, 3 design partners, enterprise wrapper) → only #1 is dev-completable autonomously. #2 needs founder outreach. #3 needs capital.

The session has been ruthlessly focused on dev-only progress. ~3 commits shipped, 510/510 tests pass, no regressions.

**Pacing note**: D5 (SaaS shell, +0.443 weighted) is alone bigger than ALL other dev-only lifts combined for D2+D7+D8+D3-partial. Should consider doing D5 SECOND-to-last (after dependencies in D7/D8 are stable) since it's the biggest lever.
</context>

<next_action>
Resume with D7 LICENSE file. Specific first command:

```bash
# Step 1: Write LICENSE file with BSL 1.1 + MIT Change License clause
# Reference: https://mariadb.com/bsl11/
# Change Date: 2030-05-15 (4 years from selection date 2026-05-15)
# Change License: MIT
# Additional Use Grant: any non-commercial use; any commercial use up to <threshold>
```

Then in order:
1. Update `pyproject.toml` license field: `"Proprietary"` → `"BUSL-1.1"` (SPDX)
2. Write `THREAT_MODEL.md` with formal STRIDE table (extends SECURITY.md)
3. Add `.github/workflows/security.yml` with Bandit + safety + pip-audit (advisory)
4. Add SLSA L3 provenance to `publish.yml`
5. Add Sigstore signing to release flow
6. Run `python -m pytest -q` + commit D7 batch

Then advance to D8 (international expansion). Then D2 (lineage PDF). Then D6 + D5.

Re-run triple-AI IC review at end of session to measure actual lift.

**File pointers for resume**:
- PRD: `PRD_v11_score_to_8.md`
- IC baseline: `TRIPLE_REVIEW_2026-05-15.md`, `EXTERNAL_IC_REVIEW_2026-05-15.md`
- Last commit: `8175aad` (MOAT_SWEEP)
- Test sweep: `python -m pytest -q --tb=no` (expect 510 pass)
- Live data check: `python scripts/audit_listed.py AAPL --sleep 0.5` (expect mcap_dev=-36%)
- Moat sweep: `python scripts/moat_sweep.py` (expect 8/14 pass)

**Cleanup before resume** (optional): delete `AUDIT_RUN_LISTED_smoke.md` + `.json` (test artifacts, untracked).
</next_action>
